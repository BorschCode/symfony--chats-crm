# .env file

# In all environments, the following files are loaded if they exist,
# the latter taking precedence over the former:
#
#  * .env                contains default values for the environment variables needed by the app
#  * .env.local          uncommitted file with local overrides
#  * .env.$APP_ENV       committed environment-specific defaults
#  * .env.$APP_ENV.local uncommitted environment-specific overrides

# Real environment variables win over .env files.
#
# DO NOT DEFINE PRODUCTION SECRETS IN THIS FILE NOR IN ANY OTHER COMMITTED FILES.
# https://symfony.com/doc/current/configuration/secrets.html
#
# Run "composer dump-env prod" to compile .env files for production use (requires symfony/flex >=1.2).
# https://symfony.com/doc/current/best_practices.html#use-environment-variables-for-infrastructure-configuration

###> symfony/framework-bundle ###
APP_ENV=dev
APP_SECRET=25732159074218659087234609823465
###< symfony/framework-bundle ###

###> docker-configuration ###
# Select which ports should be exposed to the host machine
# -------------------------------------------------------
# Standard Web Ports (Modified for 8059/8459)
HTTP_PORT=8059
HTTPS_PORT=8459
HTTP3_PORT=8459

# Database Ports (Modified for non-standard)
# Note: Ensure your compose.override.yaml maps these, e.g., "33069:3306"
MYSQL_PORT=33069
POSTGRES_PORT=54329

# Caddy Server Name
# If you change ports, internal Caddy still listens on internal 80/443, 
# so localhost is usually fine, but you access via localhost:8059
SERVER_NAME=localhost
###< docker-configuration ###

###> doctrine/doctrine-bundle ###
# Format for MySQL: mysql://db_user:db_password@127.0.0.1:3306/db_name?serverVersion=8.0&charset=utf8mb4
# Format for PostgreSQL: postgresql://symfony:ChangeMe@127.0.0.1:5432/app?serverVersion=16&charset=utf8
#
# IMPORTANT: When running INSIDE Docker, the host is "database" (service name), 
# and the port is the INTERNAL port (3306 or 5432), NOT the external one defined above.
#
DATABASE_URL="mysql://app:!ChangeMe!@database:3306/app?serverVersion=8.0.32&charset=utf8mb4"
###< doctrine/doctrine-bundle ###

###> symfony/messenger ###
# MESSENGER_TRANSPORT_DSN=doctrine://default?auto_setup=0
###< symfony/messenger ###

###> symfony/mailer ###
# MAILER_DSN=null://null
###< symfony/mailer ###
